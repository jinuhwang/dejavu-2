# @package _global_

# to execute this experiment run:
# python train.py experiment=example

defaults:
  - nextqa-hard

model:
  reuse_model_name: "95"

data:
  test_refresh_interval: 0

ckpt_path: ${paths.data_dir}/checkpoints/nextqa/nextqa-95.ckpt
